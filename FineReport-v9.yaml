id: FineReport-v9

info:
  name: 帆软v9任意文件覆盖
  author: Pikka
  severity: high

requests:
  - method: GET
    path: 
      - "{{BaseURL}}/WebReport/update.jsp"
    
    matchers-condition: and
    matchers: 
      - type: status
        status: 
          - 200
    